# Fixture clusters deliberately omit the OLMv0 controllers. The migrated
# Subscription snapshots still reference this source, so retain the source
# object as committed fixture input for catalog migration and resolution.
apiVersion: operators.coreos.com/v1alpha1
kind: CatalogSource
metadata:
  name: operatorhubio-catalog
  namespace: olm
spec:
  sourceType: grpc
  # Verified from the bootstrap operatorhubio ClusterCatalog on 2026-09-16.
  # Keep this immutable reference available in Quay so fixture replay is
  # independent of OLMv0 and does not follow the mutable `latest` tag.
  image: quay.io/operatorhubio/catalog@sha256:8551d422e0395247660e1f5090d93a5376a07514bf98d26c05431aa0ef5a42a3
